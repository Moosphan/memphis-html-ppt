# juejin-skill：把文章发布到掘金做成可复用的 Skill

很多工具的起点都不宏大，往往只是为了去掉一段反复出现、但没有创造性的劳动。juejin-skill 也是这样长出来的。

## 为什么要做这个工具

本地写 Markdown，配图和草稿都按项目目录管理。文章完成后，回到掘金编辑器里补分类、挑标签、上传图片、创建草稿，偶尔做一次问题不大，但写作频率一稳定，这条链路就迅速变成额外负担。

- 登录态最好能复用，而不是每次重新处理登录流程
- 分类、标签和热榜信息应该可以直接查询，还能给出合理推荐
- 本地 Markdown 应该能够直接转换成平台草稿
- 本地图片需要在发布前完成上传和替换
- 已发布文章需要支持基于本地稿件继续更新
- 真正公开发布时，应该保留一次明确确认

## 核心思路：先把能力收敛到 CLI

做 Agent Skill 时，很容易先写提示词让模型去判断。但随着功能变多，维护成本上升很快。更稳妥的做法是先把确定性的动作收敛到 CLI，再让不同平台的 Skill 文档去调用这套命令。

- login：浏览器登录，提取会话 Cookie
- verify：校验当前登录状态
- categories：查询平台分类列表
- tags：查询指定分类下的推荐标签
- hot：查询全站热门文章
- publish：发布文章到掘金
- update：更新已发布文章
- download：下载文章到本地

CLI 职责尽量简单：解析参数、读取文件、组装调用、输出结果。真正的业务逻辑都下沉到 core 层。

## HTTP 层要薄，错误要早暴露

掘金的核心操作最终都落在 HTTP 请求上。刻意没有做太厚的封装，比如自动重试、业务错误猜测、静默降级等。

- 和第三方站点打交道时，接口变化、登录失效、参数缺失对应完全不同的问题
- 越早看到状态码和响应片段，排查就越快
- 如果客户端层过度"聪明"，出问题时反而不容易定位
- 保留 fetchImpl 注入点，让测试更干净

端点和配置统一收敛在 config.js。平台调整接口时，只改配置层，维护成本低很多。

## 登录交给浏览器，会话交给本地文件

对用户来说，最自然的体验是用浏览器完成正常登录，然后让程序复用登录结果。login 命令用 Playwright 启动浏览器，登录成功后提取会话 Cookie，保存到本地文件。

- 工具本身不接触账号密码，不需要处理扫码、验证码
- 用户登录一次后，发布、更新和上传图片都复用同一份会话
- 会话失效时，只需重新执行 login
- 撤销登录状态，删除 session.json 即可

verify 命令用当前会话校验登录状态。很多"发布失败"表面看像参数问题，最后往往只是会话过期。

## 把分类、标签和热榜纳入同一工作流

发文章不是只把正文上传出去。还要确认当前有哪些分类、某个分类下有哪些推荐标签、最近热门内容是什么。

- categories：查询平台分类列表
- tags：查询指定分类下的推荐标签，支持关键词过滤
- hot：查询全站或指定分类下的热门文章

平台表面上"能用"的接口，不一定是最适合自动化调用的接口。需要顺着真实页面行为反推更可靠的数据入口。如果用户没有显式指定分类，CLI 会根据标题和正文关键词给出推荐。

## 发布默认只创建草稿

发布逻辑的核心流程：读取 Markdown、提取标题、移除标题行避免重复、扫描本地图片完成上传、生成摘要、渲染为 HTML、创建草稿。只有在明确确认后，才执行公开发布。

- 默认行为是创建草稿而不是直接公开
- 要真正发布，必须显式传入 --publish --confirm-publish
- 自动化适合承担重复劳动，但公开发布需要作者明确授权
- 标题提取只认第一个一级标题，本地文稿保持完整

## 本地图片上传是最容易出问题的部分

本地 Markdown 里的图片引用在预览时完全正常，但直接提交到平台，线上读者无法访问相对路径。图片上传几乎是最容易出问题、也最必须做好的部分。

- 识别 Markdown 中的图片链接，只处理本地文件路径
- 远程图片、data: 图片直接跳过
- 同一张图片多次出现时，只上传一次
- 走平台当前网页编辑器使用的媒体上传链路
- 正文内容和图片元数据要同时更新

## 更新文章时不要碰用户没让你碰的东西

update 命令解决的是另一个高频问题：如何让本地 Markdown 安全地同步回线上文章。

- 默认行为依旧是更新草稿
- 进一步同步到公开文章需要显式确认
- 分类、标签、封面、摘要、主题、原创状态应该继承原值
- 用户只是想修一张图、改一个段落，不应该把封面清空、标签弄丢
- 标识字段沿用字符串表示，降低精度风险

## 下载能力补齐了内容闭环

除了发布和更新，还补了下载命令。线上文章可以回收到本地 Markdown，本地 Markdown 也可以重新发布和继续更新。

- 下载单篇文章：优先获取平台 Markdown，退回到浏览器抓取 HTML 转换
- 下载用户文章：批量下载指定用户的文章
- 带 --download-images 时，远程图片一并下载到本地
- 本地原稿、图片和历史版本始终掌握在自己手里

## Codex 和 Claude Code 共用一套运行时

同时服务两个平台，但没有各维护一套逻辑，而是共用同一套 Node.js CLI。

- Codex 侧依赖 SKILL.md 描述触发条件和安全边界
- Claude Code 侧在 .claude/commands/ 下定义命令文档
- 两边文档格式不同，但最终调用同一个入口
- 功能只实现一次，测试围绕同一套核心代码

## 工程化一起补齐

如果一个 Skill 只能在作者自己的机器上工作，本质上还只是个人脚本。要让它被复用，就必须把版本发布和交付路径做完整。

- 推送标签后 GitHub Actions 自动完成构建和发布
- 安装依赖和浏览器运行环境
- 运行 lint 和测试
- 构建两套产物
- 生成版本说明并上传 Release

## 测试守住的是自己的边界

测试覆盖了认证、标签、图片上传、发布更新、下载和分类推荐等核心能力。没有试图证明"平台永远不会变"，而是守住自己定义的边界和规则。

- 同一张本地图片重复出现时只上传一次
- 远程图片不会被误判为本地文件
- 更新文章时保留原有元信息
- 没有明确确认时只停留在草稿阶段
- 出故障时能更快区分是平台问题还是自己的问题

## 最后的边界感

juejin-skill 已经不再只是一个"帮我把文章发出去"的脚本，而是一条完整的内容工作流：查分类、查标签、读 Markdown、上传图片、创建草稿、更新文章、下载归档。

重复操作交给工具，关键决策仍然留给作者。分类可以推荐，图片可以自动上传，草稿可以自动创建；但要不要公开发布、要不要覆盖线上内容，依然需要明确确认。
